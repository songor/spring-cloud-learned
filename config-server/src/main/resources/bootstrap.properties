# 加密解密
# {"description":"The encryption algorithm is not strong enough","status":"INVALID"}
# The solution is just create bootstrap.properties file in config server and add encrypt.key property.
encrypt.key=encrypt